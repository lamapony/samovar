# Руководство по интеграции Spaced Repetition System (SRS)

## Обзор

Система интервального повторения (SRS) с алгоритмом SuperMemo 2 (SM-2) - это самая сложная и продвинутая функция портала Samovar. Она обеспечивает персонализированное обучение с оптимальными интервалами повторения для каждого слова.

## Архитектура системы

### Основные компоненты

1. **SM2Algorithm** - Ядро алгоритма SuperMemo 2
   - Расчет интервалов повторений
   - Определение приоритетов карточек
   - Адаптация к индивидуальной памяти

2. **VocabCard** - Модель данных словарной карточки
   - Хранение информации о слове
   - Отслеживание статистики повторений
   - Расчет точности запоминания

3. **SRSManager** - Менеджер системы
   - Управление коллекцией карточек
   - Формирование очереди повторений
   - Сохранение/загрузка прогресса
   - Статистика и аналитика

4. **SRSReviewSession** - UI компонент сессии
   - Интерактивный интерфейс повторения
   - Анимации и обратная связь
   - Интеграция с TTS

## Алгоритм SM-2

### Принцип работы

Алгоритм использует 4 параметра:
- **Quality (0-5)** - Качество воспоминания
- **Repetitions** - Количество успешных повторений
- **Easiness (≥1.3)** - Фактор легкости
- **Interval** - Интервал до следующего повторения (в днях)

### Формула расчета

```javascript
newEasiness = max(1.3, easiness + (0.1 - (5 - quality) * (0.08 + (5 - quality) * 0.02)))

if (quality < 3) {
    // Провал - сброс прогресса
    repetitions = 0
    interval = 1
} else {
    repetitions++
    if (repetitions == 1) interval = 1
    else if (repetitions == 2) interval = 6
    else interval = round(interval * easiness)
}
```

### Система оценок

- **0-2 (Провал)**: Не помню → Интервал сбрасывается
- **3 (Сложно)**: Помню с трудом → Короткий интервал
- **4 (Хорошо)**: Помню нормально → Стандартный интервал
- **5 (Легко)**: Помню отлично → Увеличенный интервал

## Интеграция в уроки

### Шаг 1: Подключение файлов

```html
<!-- В <head> -->
<link rel="stylesheet" href="../css/srs.css">

<!-- Перед </body> -->
<script src="../js/spaced-repetition.js"></script>
```

### Шаг 2: Инициализация менеджера

```javascript
// Создать глобальный экземпляр SRS Manager
const srsManager = new SRSManager();
```

### Шаг 3: Импорт словаря урока

```javascript
// Пример: импорт слов из урока 01
const lesson01Words = [
    {
        russian: 'Привет',
        danish: 'Hej',
        transcription: 'privʲet',
        lessonId: '01-intro',
        tags: ['greeting', 'informal']
    },
    {
        russian: 'Здравствуйте',
        danish: 'Goddag',
        transcription: 'zdravstvujtʲe',
        lessonId: '01-intro',
        tags: ['greeting', 'formal']
    }
    // ... остальные слова
];

// Импорт в SRS
srsManager.importFromLesson('01-intro', lesson01Words);
```

### Шаг 4: Добавление кнопки "Добавить в SRS"

```html
<div class="lesson-actions">
    <button class="btn btn-primary" id="add-to-srs-btn">
        🧠 Добавить слова в SRS
    </button>
</div>

<script>
document.getElementById('add-to-srs-btn').addEventListener('click', () => {
    const imported = srsManager.importFromLesson('01-intro', lesson01Words);
    alert(`Добавлено ${imported.length} слов в систему повторений!`);
});
</script>
```

### Шаг 5: Встроенная сессия повторения

```html
<section class="srs-review-section">
    <h2>Повторение слов урока</h2>
    <div id="lesson-srs-container"></div>
</section>

<script>
// Создать сессию только для слов текущего урока
const lessonCards = srsManager.getCardsByLesson('01-intro');
if (lessonCards.length > 0) {
    new SRSReviewSession('lesson-srs-container', srsManager, {
        sessionSize: lessonCards.length,
        showProgress: true,
        autoSpeak: true
    });
}
</script>
```

## API Reference

### SRSManager

#### Методы управления карточками

```javascript
// Добавить одну карточку
srsManager.addCard({
    russian: 'Привет',
    danish: 'Hej',
    transcription: 'privʲet',
    lessonId: '01-intro',
    tags: ['greeting']
});

// Добавить несколько карточек
srsManager.addCards([card1, card2, card3]);

// Найти карточку по русскому слову
const card = srsManager.findCardByRussian('Привет');

// Получить карточку по ID
const card = srsManager.getCard(cardId);

// Обновить карточку
srsManager.updateCard(cardId, { tags: ['greeting', 'basic'] });

// Удалить карточку
srsManager.deleteCard(cardId);
```

#### Методы очереди повторений

```javascript
// Получить карточки, которые нужно повторить
const dueCards = srsManager.getDueCards(limit);

// Получить новые карточки
const newCards = srsManager.getNewCards(limit);

// Получить полную очередь (новые + повторения)
const queue = srsManager.getReviewQueue(includeNew);
```

#### Обработка повторений

```javascript
// Записать результат повторения
const result = srsManager.reviewCard(cardId, quality);
// result = {
//     card: VocabCard,
//     wasNew: boolean,
//     nextReview: timestamp,
//     interval: days,
//     easiness: number
// }
```

#### Статистика

```javascript
// Получить общую статистику
const stats = srsManager.getStats();
// stats = {
//     total: number,
//     new: number,
//     learning: number,
//     reviewing: number,
//     due: number,
//     totalReviews: number,
//     overallAccuracy: percentage,
//     avgEasiness: number,
//     dailyStats: {...},
//     settings: {...}
// }

// Получить карточки по уроку
const lessonCards = srsManager.getCardsByLesson('01-intro');

// Получить карточки по тегу
const greetingCards = srsManager.getCardsByTag('greeting');

// Получить процент удержания за период
const retention7d = srsManager.getRetentionRate(7); // за 7 дней
const retention30d = srsManager.getRetentionRate(30); // за 30 дней
```

#### Импорт/Экспорт

```javascript
// Импорт из урока
srsManager.importFromLesson(lessonId, words);

// Экспорт данных
const data = srsManager.exportData();

// Импорт данных
srsManager.importData(data);

// Сброс прогресса карточки
srsManager.resetCard(cardId);

// Сброс всего прогресса
srsManager.resetAllProgress();
```

### VocabCard

#### Свойства

```javascript
card.id              // Уникальный ID
card.russian         // Русское слово
card.danish          // Датский перевод
card.transcription   // Транскрипция
card.example         // Пример использования
card.exampleDanish   // Перевод примера
card.lessonId        // ID урока
card.tags            // Массив тегов

// SRS параметры
card.repetitions     // Количество успешных повторений
card.easiness        // Фактор легкости (1.3-2.5+)
card.interval        // Интервал в днях
card.lastReview      // Timestamp последнего повторения
card.nextReview      // Timestamp следующего повторения
card.totalReviews    // Всего повторений
card.correctReviews  // Правильных повторений
```

#### Методы

```javascript
// Проверить, нужно ли повторять
card.isDue(now);

// Получить приоритет в очереди
card.getPriority(now);

// Получить точность запоминания (%)
card.getAccuracy();

// Получить статус ('new', 'learning', 'reviewing')
card.getStatus();

// Записать повторение
card.review(quality);
```

### SRSReviewSession

#### Создание сессии

```javascript
const session = new SRSReviewSession(containerId, srsManager, {
    sessionSize: 20,        // Количество карточек в сессии
    showProgress: true,     // Показывать прогресс-бар
    autoSpeak: true         // Автоматическое произношение
});
```

## Примеры использования

### Пример 1: Базовая интеграция в урок

```html
<!DOCTYPE html>
<html lang="da">
<head>
    <meta charset="UTF-8">
    <title>Урок 01 - Приветствия</title>
    <link rel="stylesheet" href="../css/beginner.css">
    <link rel="stylesheet" href="../css/srs.css">
</head>
<body>
    <main>
        <h1>Урок 01: Приветствия</h1>
        
        <!-- Обычное содержание урока -->
        <section id="vocabulary">
            <!-- ... словарь ... -->
        </section>
        
        <!-- SRS секция -->
        <section class="srs-section">
            <h2>🧠 Интервальное повторение</h2>
            <p>Добавьте слова этого урока в систему повторений для долгосрочного запоминания.</p>
            <button class="btn btn-primary" id="add-to-srs">
                Добавить в SRS
            </button>
            <div id="srs-status"></div>
        </section>
    </main>

    <script src="../js/lesson-engine.js"></script>
    <script src="../js/spaced-repetition.js"></script>
    <script>
        const srsManager = new SRSManager();
        
        const lessonWords = [
            { russian: 'Привет', danish: 'Hej', transcription: 'privʲet', lessonId: '01-intro' },
            { russian: 'Пока', danish: 'Farvel', transcription: 'paka', lessonId: '01-intro' }
        ];

        document.getElementById('add-to-srs').addEventListener('click', () => {
            const imported = srsManager.importFromLesson('01-intro', lessonWords);
            document.getElementById('srs-status').innerHTML = `
                <div class="success-message">
                    ✅ Добавлено ${imported.length} слов в систему повторений!
                    <a href="srs-demo.html" class="btn">Начать повторение</a>
                </div>
            `;
        });
    </script>
</body>
</html>
```

### Пример 2: Встроенная мини-сессия

```javascript
// Создать мини-сессию для быстрого повторения
function createQuickReview() {
    const dueCards = srsManager.getDueCards(5); // Только 5 карточек
    
    if (dueCards.length === 0) {
        alert('Нет карточек для повторения!');
        return;
    }
    
    new SRSReviewSession('quick-review-container', srsManager, {
        sessionSize: 5,
        showProgress: false,
        autoSpeak: true
    });
}
```

### Пример 3: Статистика по уроку

```javascript
function showLessonStats(lessonId) {
    const cards = srsManager.getCardsByLesson(lessonId);
    const stats = {
        total: cards.length,
        new: cards.filter(c => c.getStatus() === 'new').length,
        learning: cards.filter(c => c.getStatus() === 'learning').length,
        mastered: cards.filter(c => c.getStatus() === 'reviewing').length,
        avgAccuracy: cards.reduce((sum, c) => sum + c.getAccuracy(), 0) / cards.length
    };
    
    console.log(`Урок ${lessonId}:`, stats);
    return stats;
}
```

## Настройки системы

### Изменение параметров

```javascript
// Изменить количество новых карточек в день
srsManager.settings.newCardsPerDay = 30;

// Изменить максимум повторений в день
srsManager.settings.reviewsPerDay = 150;

// Сохранить изменения
srsManager.save();
```

## Оптимизация производительности

### LocalStorage

Система использует localStorage для хранения данных. Рекомендации:
- Регулярно экспортировать данные для резервного копирования
- Ограничить количество карточек до 5000 для оптимальной производительности
- Периодически очищать старые неиспользуемые карточки

### Индексация

Для быстрого поиска карточек используется Map:
```javascript
// O(1) поиск по ID
const card = srsManager.cards.get(cardId);

// O(n) поиск по русскому слову (используйте с осторожностью)
const card = srsManager.findCardByRussian('Привет');
```

## Расширенные возможности

### Кастомизация алгоритма

Можно настроить параметры SM-2:

```javascript
// В SRSManager.settings
srsManager.settings.easyBonus = 1.3;      // Множитель для "легких" карточек
srsManager.settings.hardInterval = 1.2;   // Множитель для "сложных" карточек
srsManager.settings.lapseInterval = 0.5;  // Множитель при провале
```

### Интеграция с аналитикой

```javascript
// Отслеживание событий
srsManager.onChange = (event) => {
    console.log('SRS Event:', event);
    // Отправить в аналитику
    if (typeof gtag !== 'undefined') {
        gtag('event', 'srs_review', {
            event_category: 'learning',
            event_label: event.type
        });
    }
};
```

## Troubleshooting

### Проблема: Карточки не сохраняются

**Решение**: Проверьте localStorage:
```javascript
console.log(localStorage.getItem('ruslaering_srs_v1'));
```

### Проблема: Слишком много карточек в очереди

**Решение**: Ограничьте размер сессии:
```javascript
const session = new SRSReviewSession('container', srsManager, {
    sessionSize: 10 // Меньше карточек за раз
});
```

### Проблема: Интервалы слишком короткие/длинные

**Решение**: Настройте easiness factor:
```javascript
// Для более агрессивного обучения
card.easiness = 2.0; // Меньше = чаще повторения

// Для более расслабленного обучения
card.easiness = 2.8; // Больше = реже повторения
```

## Лучшие практики

1. **Регулярность**: Поощряйте пользователей заниматься ежедневно
2. **Размер сессии**: 15-25 карточек оптимально для одной сессии
3. **Баланс**: 70% повторений, 30% новых карточек
4. **Честность**: Призывайте оценивать себя честно
5. **Контекст**: Добавляйте примеры использования к карточкам

## Будущие улучшения

- [ ] Синхронизация между устройствами
- [ ] Графики прогресса и аналитика
- [ ] Режим "cramming" для быстрого повторения
- [ ] Аудио-карточки для произношения
- [ ] Игровые элементы (достижения, стрики)
- [ ] Экспорт в Anki формат
- [ ] Машинное обучение для персонализации алгоритма

## Заключение

Система интервального повторения - это мощный инструмент для долгосрочного запоминания. При правильной интеграции она может значительно улучшить эффективность обучения на платформе Samovar.

Для вопросов и поддержки: см. документацию или исходный код в `js/spaced-repetition.js`.
